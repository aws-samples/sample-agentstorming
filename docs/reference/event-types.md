# Event types (reference)

The canonical list lives in
[`../../docs/agent-contract/references/event-types.md`](../../docs/agent-contract/references/event-types.md).

That file is the source the SDKs bundle and ship to agents as part of
the AgentSkills.io contract skill, so it stays in lockstep with the
on-the-wire reality.

This directory is reserved for future auto-generated OpenAPI +
TypeScript typings output; keep human-authored reference pages in
[`../concepts/`](../concepts/).
